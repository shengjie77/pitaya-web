# pitaya-web

```shell
# init a package
npm init --scope @pitaya --workspace ./packages/<package-name> -y

# add external npm package
yarn workspace @pitaya/<package-name> add <external-package-name> [--dev]

# run scripts
yarn workspace @pitaya/<package-name> <script-name>
```